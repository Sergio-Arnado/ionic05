import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ionic05',
  appName: 'ionic05',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
